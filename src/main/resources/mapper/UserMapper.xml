<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.isp.dao.UserDao">

    <resultMap id="UserBaseMap" type="com.isp.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="account" property="account" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <!--<result column="role" property="role" jdbcType="VARCHAR"/>-->
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <association property="rolee" javaType="com.isp.entity.Role">
            <result column="name" property="name"></result>
        </association>
    </resultMap>

    <select id="getUserByPage" parameterType="java.lang.Integer" resultMap="UserBaseMap">
        SELECT user.id, account, username, role.name, STATUS, email FROM USER, role WHERE USER.role = role.id
        ORDER BY id limit ${start}, ${end}
    </select>

    <select id="getUserByAccount" parameterType="java.lang.String" resultMap="UserBaseMap">
        SELECT user.id, account, username, role.name, STATUS, email FROM USER, role WHERE USER.role = role.id
        and account = '${account}'
    </select>
    
    <select id="getTotalNumber" resultType="java.lang.Integer">
        SELECT COUNT(id) FROM USER
    </select>

    <insert id="insertUser">
        INSERT INTO USER (account, password, username, role, status, email) VALUE ('${account}','${password}',
        '${username}','${role}','${status}','${email}')
    </insert>
</mapper>